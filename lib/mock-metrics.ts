// Illustrative figures for the marketing page's "what the daily email looks
// like" preview on the landing page.
//
// This is NOT dashboard data. The dashboard (/dashboard) reads real telemetry
// pushed by the agent and shows an honest empty state when there is none — it
// deliberately does not import anything from this file. Keeping these numbers
// here is fine for a product screenshot; showing them as though a server had
// reported them would not be.

export const currentStats = {
  temperature: 51.4,
  uptime: 99.97,
  latency: 8.6,
  downloadSpeed: 842,
  uploadSpeed: 431,
}
